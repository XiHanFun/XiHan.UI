import{_ as t,o as s,c as i,ak as a}from"./chunks/framework.DxrHb2fb.js";const k=JSON.parse('{"title":"表单参与与重置","description":"","frontmatter":{},"headers":[],"relativePath":"guide/forms.md","filePath":"guide/forms.md","lastUpdated":1789465551000}'),o={name:"guide/forms.md"};function d(l,e,n,p,c,h){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="表单参与与重置" tabindex="-1">表单参与与重置 <a class="header-anchor" href="#表单参与与重置" aria-label="Permalink to &quot;表单参与与重置&quot;">​</a></h1><p>复合控件的值保存在自己的状态机中，浏览器不可见。要让它随 <code>&lt;form&gt;</code> 一起提交、一起重置，依靠两件事：一份表单影子（把值映射为原生输入）和一条重置事件（把宿主表单的 <code>reset</code> 转换进状态机）。</p><h2 id="表单影子" tabindex="-1">表单影子 <a class="header-anchor" href="#表单影子" aria-label="Permalink to &quot;表单影子&quot;">​</a></h2><p>提供 <code>name</code> 才生成影子，未提供时整个组件不参与提交，既有 DOM 不变。</p><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">XhSwitch</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;notify&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> default-checked</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>提交结果是 <code>notify=on</code>。值可以更换：<code>&lt;XhSwitch name=&quot;theme&quot; value=&quot;dark&quot; /&gt;</code> 提交 <code>theme=dark</code>。</p><p>三种形态，按组件的值形状划分：</p><table tabindex="0"><thead><tr><th>形态</th><th>使用者</th><th>说明</th></tr></thead><tbody><tr><td>单个 <code>hidden-input</code></td><td>color-picker、combobox、tree-select、checkbox、switch、rating 等</td><td>多值按逗号拼接为一串</td></tr><tr><td>一值一个影子输入</td><td>checkbox-group、radio-group</td><td>与原生 checkbox / radio 同构，依靠 <code>checked</code> 表达选中</td></tr><tr><td>隐藏 <code>&lt;select&gt;</code></td><td>select</td><td>多选直接开启原生 <code>multiple</code></td></tr></tbody></table><p>勾选类控件的语义与原生一致：未勾选时整个字段不进入 <code>FormData</code>，不是提交空值。checkbox 的半选（<code>indeterminate</code>）按未勾选处理，原生中它也只是外观，是否提交由 <code>checked</code> 决定。</p><p>影子在各适配器中的来源不同：</p><ul><li>Vue：单体控件（checkbox、switch）由组件自行渲染，提供 <code>name</code> 后才有该节点；有子部件的组件由作者写出对应部件，例如 <code>&lt;XhComboboxHiddenInput /&gt;</code>。</li><li>Web Components：一律由作者编写 <code>data-xh-part=&quot;hidden-input&quot;</code> 的原生节点，元素只负责写入属性。</li></ul><div class="tip custom-block"><p class="custom-block-title">为什么 <code>type=hidden</code> 可以放进 <code>&lt;button&gt;</code></p><p>checkbox 与 switch 的根是 <code>&lt;button&gt;</code>，HTML 的内容模型禁止 button 含有交互内容后代。<code>&lt;input type=&quot;hidden&quot;&gt;</code> 不在此列：规范中 input 的条目写明“type 属性不处于 Hidden 状态时”才算交互内容，因此这种嵌套是合法的。</p></div><h2 id="表单重置" tabindex="-1">表单重置 <a class="header-anchor" href="#表单重置" aria-label="Permalink to &quot;表单重置&quot;">​</a></h2><p>带 <code>name</code> 的组件都响应表单重置。点击 <code>&lt;button type=&quot;reset&quot;&gt;</code>（或调用 <code>form.reset()</code>）时，它们各自回到自己的默认值，与旁边的原生输入框一致。</p><p>重置桥按事件目标的原生 HTMLElement 品牌与 <code>form</code> 节点名识别表单，不依赖顶层 <code>HTMLFormElement</code> 构造器。因此组件与表单位于 iframe 中，或表单从另一 Window adopt 到当前 Document 后，仍会跟随所属表单重置；普通元素派发的同名事件不会冒充表单。</p><p>对于正式提供 <code>form</code> 属性的组件，重置桥优先按该 ID 查找组件所属 Document 或 ShadowRoot 中的表单；未提供时才关联最近的祖先表单。空 ID、目标不存在或目标不是表单时均不关联其他表单。关联在重置发生时读取，因此目标表单延后创建或属性变更不需要重新挂载组件；取消原生 <code>reset</code> 事件仍会取消组件重置。此规则不使未声明 <code>form</code> 的组件自动获得该公共属性。</p><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">form</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;XhRadioGroupRoot name=&quot;plan&quot; default-value=&quot;standard&quot;&gt;…&lt;/XhRadioGroupRoot&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;XhRatingRoot name=&quot;score&quot; :default-value=&quot;3&quot;&gt;…&lt;/XhRatingRoot&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;button type=&quot;reset&quot;&gt;重置&lt;/button&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">form</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>几条要点：</p><ul><li>落点按当前的 props 重新计算，不是挂载时冻结的值。宿主更换 <code>defaultValue</code>（例如切换到编辑另一条记录）后，重置回到新的默认值，与原生 <code>reset()</code> 回到当前 default 一致。</li><li>受控组件只发出意图。组件不自行修改状态，只调用一次 <code>onValueChange</code>（或 <code>onCheckedChange</code>），由宿主写回。</li></ul><div class="warning custom-block"><p class="custom-block-title">受控组件要响应重置，必须显式传 <code>defaultValue</code></p><p>这是本库与“受控 reset 是空操作”的分歧点，也是最容易出错的一条。</p><p>组件内部的 <code>?? 兜底</code> 把宿主声明的默认值和组件的空值写在同一个表达式中（radio-group 是 <code>null</code>、rating 是 <code>0</code>、tags-input 是空数组）。受控且未写 <code>defaultValue</code> 时，组件不做任何动作、不发出任何意图；否则该空值会被当作默认值发给宿主，重置就变成了清空数据。</p></div><ul><li>重置被拦截时不动作。表单侧 <code>event.preventDefault()</code> 之后，同表单的原生控件也未还原，组件单方面还原会产生半份默认值。</li><li>归属在事件发生时计算。监听挂在组件所在的根节点上而不是 <code>&lt;form&gt;</code> 上（表单会被条件渲染替换、组件也会被移动），用 <code>closest(&#39;form&#39;)</code> 比对，因此嵌套表单不会误伤。</li></ul><p>不在任何表单内、无 DOM 的服务端、作者未写影子输入，三种情形都不需要特别处理：归属判定不命中、服务端不挂载副作用、锚点是组件根节点而不是影子输入。</p><h2 id="控件在薄封装内" tabindex="-1">控件在薄封装内 <a class="header-anchor" href="#控件在薄封装内" aria-label="Permalink to &quot;控件在薄封装内&quot;">​</a></h2><p><code>XhFieldControl</code> 默认把接线属性（<code>id</code> 与各条 <code>aria-*</code>）合并到它唯一的子节点上。子节点是组件时，合并到的是组件的根，而薄封装的根往往是 <code>div</code>。</p><p>标签的 <code>for</code> 只对可标注元素生效（<code>input</code> / <code>select</code> / <code>textarea</code> / <code>button</code> 等），指向 <code>div</code> 时没有任何效果，且不报错。</p><p>点击标题这一半由库处理：<code>for</code> 未落到实处时，字段会把焦点送给控件内第一个可 tab 的节点，无论封装是库内的还是自行编写的。读屏能否读出名称是另一半，取决于以下两种写法。</p><p>一、控件的根就是可聚焦元素：不需要额外处理，默认路径正确：</p><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">XhFieldControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;input type=&quot;email&quot; /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">XhFieldControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>二、控件位于封装内：关闭 <code>asChild</code>，让封装内部自行获取：</p><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">XhFieldControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">as-child</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;MyInput /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">XhFieldControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// MyInput 内部</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { useFieldControl } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;@xihan-ui/vue&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> controlProps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useFieldControl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 绑定到真正可聚焦的节点上</span></span></code></pre></div><p><code>useFieldControl</code> 在字段外调用返回空对象，封装仍可单独使用。不关闭 <code>asChild</code> 时属性会被合并两次，一次在封装根、一次在真控件，页面上会出现两个相同的 <code>id</code>。</p><p>库自身的控件不需要处理这一层。select、text-field、date-picker 等封装内部已经把两份接线取到真正可聚焦的部件上：说明与校验状态一份，字段的标签一份。直接放入 <code>XhFieldControl</code> 即可，<code>asChild</code> 保持默认。</p><p>标签那份是并入而不是覆盖：字段的标签排在最前，控件自己的部分（下拉的当前值等）跟在后面，两者都能读出。控件自带的 <code>aria-labelledby</code> 指向它自己的 <code>label</code> 部件，使用字段的标签时该部件未渲染，只保留它会形成悬空引用：按 accname 规则跳过，名称又无法回退到 <code>for</code>（<code>for</code> 指向封装根的 <code>div</code>），焦点所在的控件就没有名称。</p><h2 id="参与的组件" tabindex="-1">参与的组件 <a class="header-anchor" href="#参与的组件" aria-label="Permalink to &quot;参与的组件&quot;">​</a></h2><p>34 个：checkbox、cascader、checkbox-group、color-field、color-picker、color-slider、color-swatch-picker、combobox、date-field、date-picker、date-range-picker、editable、field-array、file-upload、image-cropper、mention、number-field、password-input、pin-input、radio-group、rating、segmented、select、signature-pad、slider、switch、tags-input、text-field、time-field、time-picker、time-range-picker、toggle-group、transfer、tree-select。</p><p>新增的表单组件未接入重置会被门禁拦截：判据的分母从源码扫描得出（<code>types</code> 的 props 中有 <code>name?:</code> 即表单字段，字段名可以是标量字符串或 <code>FormPath</code>），不是手写名单。</p>`,37)])])}const u=t(o,[["render",d]]);export{k as __pageData,u as default};
