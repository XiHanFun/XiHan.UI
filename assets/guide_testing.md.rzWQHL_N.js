import{_ as s,o as i,c as a,ak as t}from"./chunks/framework.DxrHb2fb.js";const k=JSON.parse('{"title":"测试与质量门禁","description":"","frontmatter":{},"headers":[],"relativePath":"guide/testing.md","filePath":"guide/testing.md","lastUpdated":1789876552000}'),d={name:"guide/testing.md"};function o(c,e,n,p,l,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="测试与质量门禁" tabindex="-1">测试与质量门禁 <a class="header-anchor" href="#测试与质量门禁" aria-label="Permalink to &quot;测试与质量门禁&quot;">​</a></h1><p>框架无关的组件库最常见的问题是各适配器逐渐偏离。行为写在同一个内核中不代表各端表现一致：写入属性的时机、事件的派发形态、焦点的落点都可能不同。</p><p>XiHan.UI 的测试体系首先针对这一点。</p><h2 id="三套判据" tabindex="-1">三套判据 <a class="header-anchor" href="#三套判据" aria-label="Permalink to &quot;三套判据&quot;">​</a></h2><table tabindex="0"><thead><tr><th>入口</th><th>回答的问题</th><th>运行环境</th></tr></thead><tbody><tr><td><code>@xihan-ui/testing</code></td><td>适配器的实现是否符合规格</td><td>jsdom</td></tr><tr><td><code>@xihan-ui/testing/a11y</code></td><td>渲染结果是否存在无障碍违规</td><td>真实 Chromium</td></tr><tr><td><code>@xihan-ui/testing/position</code></td><td>浮层最终是否落在屏幕上正确的位置</td><td>真实 Chromium</td></tr></tbody></table><p>后两套必须在真实浏览器中运行：jsdom 没有布局，无法验证对比度、目标尺寸、翻面与避让。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # 第一套</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:browser</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # 后两套（先 pnpm exec playwright install chromium）</span></span></code></pre></div><p>在 Windows / macOS 宿主上，<code>pnpm test:browser</code> 固定有一条失败：像素基线文件受字体守卫拦截，整文件判失败、40 条用例全部 skipped。这是预期结果，不是环境故障；本地验证像素改动的方式见下文「像素基线」。</p><h2 id="一致性-一份规格驱动各适配器" tabindex="-1">一致性：一份规格驱动各适配器 <a class="header-anchor" href="#一致性-一份规格驱动各适配器" aria-label="Permalink to &quot;一致性：一份规格驱动各适配器&quot;">​</a></h2><p>规格（<code>ConformanceSuite</code>）声明组件的解剖部件、键盘表与用例；适配器各实现一个 <code>AdapterHarness</code>（挂载 fixture 树、获取事件、卸载）。运行器把同一份规格交给不同的 harness，逐帧采集归一化后的 <code>DomSnapshot</code> 并断言。</p><p>fixture 是框架无关的树：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FixtureNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  part</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 解剖 part 名，与 data-part 逐字相同</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tag</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  attrs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 业务属性，不含 aria- / data-scope / data-part</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  children</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FixtureNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>快照也是归一化的：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DomSnapshot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  parts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PartSnapshot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]&gt;; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// part 名 → 全部实例（文档序）</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  order</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 文档序，集合项带下标：[&#39;trigger&#39;, &#39;content&#39;, &#39;item[0]&#39;]</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  activeElement</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveElementRef</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 焦点落在哪个 part、是否恰为该元素本身</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  events</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AdapterEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 自上一帧起适配器对外派发的事件</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  strayParts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 带 data-scope 却不属于任何声明 part 的元素</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="五条契约不变量" tabindex="-1">五条契约不变量 <a class="header-anchor" href="#五条契约不变量" aria-label="Permalink to &quot;五条契约不变量&quot;">​</a></h3><p>这套判据依赖五条约束：</p><ol><li>规格零框架：套件只从 <code>@xihan-ui/core</code> / <code>@xihan-ui/headless</code> 取纯数据（解剖、键盘表、类型），不 import 任何框架；</li><li>只断言归一化快照：断言对象只能是 <code>DomSnapshot</code>，不接触组件实例、内部 ref、<code>shadowRoot</code>；</li><li>快照适配器无关：id 的具体值、<code>data-v-*</code> 等适配器痕迹在采集阶段抹除，IDREF 属性翻译为 <code>@part(...)</code>。无法抹除的差异即抽象泄漏；</li><li>单实例文档：同一时刻文档内只有一个 harness 的一个挂载实例，卸载后该 scope 不得残留节点；</li><li>状态断言归纯逻辑层：harness 不暴露当前状态机状态，<code>settle</code> 只等待可观察的 DOM 事实。</li></ol><p>第 3 条是关键：适配器之间的差异无法在归一化中抹除，说明抽象存在泄漏，应修改的是库而不是测试。</p><h3 id="分母外化" tabindex="-1">分母外化 <a class="header-anchor" href="#分母外化" aria-label="Permalink to &quot;分母外化&quot;">​</a></h3><p>键盘规格表是可达性的分母。用例通过 <code>covers</code> 字段反查行 id，缺一行即套件失败。</p><p>键盘测试只能通过补充用例通过，不能修改分母。</p><h2 id="无障碍" tabindex="-1">无障碍 <a class="header-anchor" href="#无障碍" aria-label="Permalink to &quot;无障碍&quot;">​</a></h2><p>把一致性套件的 fixture 挂载到真实 Chromium，对初始态与各用例终态运行 axe，终态按形态签名去重。</p><p>存量违规登记在 <code>tooling/testing/src/a11y/known.ts</code>：命中已登记的规则不判失败，但一条都不再命中时判定登记过期：修复后必须从表中删除，不保留已不成立的豁免。当前登记的清单见<a href="./a11y#存量违规登记表">无障碍与键盘规格</a>。</p><h2 id="浮层定位" tabindex="-1">浮层定位 <a class="header-anchor" href="#浮层定位" aria-label="Permalink to &quot;浮层定位&quot;">​</a></h2><p><code>runPositionEngine(engine, hooks, name)</code> 是一份引擎无关的契约：判据只认浮层最终出现在屏幕的位置，两个矩形都取视口坐标，因此不关心包含块关系与中间的 transform 层数。更换定位引擎实现后同样适用。</p><p>覆盖：十二种 placement 的贴边与对齐、offset、翻面与交叉轴避让（各配一条关闭后应当溢出的对照）、虚拟锚点、缩放与 transform 容器、文档滚动、容器滚动跟随、裁剪后的 <code>hidden</code>、尺寸变化重算、静置不空转、停止跟随后不再回调。</p><h2 id="像素基线" tabindex="-1">像素基线 <a class="header-anchor" href="#像素基线" aria-label="Permalink to &quot;像素基线&quot;">​</a></h2><p>计算样式不一致会被令牌快照拦截，结构错位会被一致性套件拦截，但值全部正确、外观仍然不同这一档没有其他判据覆盖：层叠顺序、私有槽的覆盖时机、两条规则的先后，只在最终位图上显现。像素基线负责这一档。</p><p>受管范围是母组件 <code>button</code> / <code>text-field</code> / <code>select</code> / <code>menu</code> / <code>popover</code> / <code>dialog</code> / <code>drawer</code> / <code>toast</code>，每件在五组轴上各出一张：<code>light×comfortable</code>、<code>dark×comfortable</code>、<code>light×compact</code>、<code>dark×compact</code>、<code>dark×more</code>。前四格是主题与密度的笛卡尔积；第五格是层叠最脆弱的一格：两块 <code>contrast</code> 取值块必须排在两个 <code>mode</code> 块之后，顺序错误即静默回归。<code>motion=reduce</code> 不在其中：静止帧与默认档没有差别，该轴由令牌快照与 <code>check-infinite-motion</code> 承担。</p><p>基线文件在 <code>packages/adapters/vue/tests/browser/__screenshots__/</code> 下，具体数量以该目录为准。</p><h3 id="必须在容器中生成的原因" tabindex="-1">必须在容器中生成的原因 <a class="header-anchor" href="#必须在容器中生成的原因" aria-label="Permalink to &quot;必须在容器中生成的原因&quot;">​</a></h3><p>基线比对的是逐像素的位图。字体、字形栅格化与子像素平滑在 Windows 与 Linux 上不同，同一份 CSS 在两边渲染出的位图不同：在开发机上生成的基线，CI 上无法匹配。</p><p>字体是最大的不确定性：全库皮肤写的是 <code>font-family: inherit</code>，没有 sans 字体令牌，字体最终取文档根上的值。截图用例因此把根上的字体族按名字固定为 <code>DejaVu Sans</code>，不交给环境默认的 <code>sans-serif</code> 解析。因此基线的成立条件不止 Linux，还包括 <code>DejaVu Sans</code> 这个名字可以解析到，即安装了 <code>fonts-dejavu-core</code>：CI 的 <code>browser</code> job 中有一步专门安装它，删除该步骤基线会整体判红。</p><p>反过来，环境中多安装字体不影响基线：字体族按名字确定，默认 <code>sans-serif</code> 落到 Noto Sans 或其他字体，画面不变。核对字体时核对的是这个名字（<code>fc-match &#39;DejaVu Sans&#39;</code>），不是默认 <code>sans-serif</code>。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> visual:baseline</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # 校验：与库里的基线逐像素比对</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> visual:baseline</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --update</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 生成 / 更新基线并写回库里</span></span></code></pre></div><p>两条都在 <code>mcr.microsoft.com/playwright:v1.62.0-noble</code> 中运行，与 CI 使用同一套渲染栈。首次运行前先 <code>docker pull</code> 该镜像；镜像版本必须与 <code>pnpm-workspace.yaml</code> 中 <code>playwright</code> 的版本一致，浏览器二进制才配套。</p><p>运行器把仓库同步到容器内的工作副本再安装依赖，不使用宿主的 <code>node_modules</code>：宿主安装的是 win32-x64 原生二进制，容器内无法运行，而在容器内安装会覆盖宿主的副本。pnpm store 与工作副本都落在一个命名卷上，重复运行不重复安装。</p><p>校验模式下基线目录不挂载进容器，容器无法写入。修改基线只能显式使用 <code>--update</code>。</p><p>比对失败时，实际截图与差异图输出到 <code>packages/adapters/vue/.vitest-attachments/</code>（不入库）。CI 上同一批文件作为 <code>visual-diffs</code> artifact 输出，可下载逐张查看。</p><h3 id="本地运行方式与固定失败项" tabindex="-1">本地运行方式与固定失败项 <a class="header-anchor" href="#本地运行方式与固定失败项" aria-label="Permalink to &quot;本地运行方式与固定失败项&quot;">​</a></h3><p><code>pnpm visual:baseline</code> 是本地唯一有效的入口，不使用宿主的渲染栈：源码同步进容器、依赖在容器内安装、用例在容器内运行，因此 Windows 与 macOS 上的结果与 CI 逐像素一致。前置条件只有 <code>docker pull</code>。</p><p>直接运行 <code>pnpm test:browser</code> 时，像素基线文件整文件判红：字体守卫在 <code>beforeAll</code> 中抛错，40 条用例全部 skipped，不生成任何位图，也不会在 <code>__screenshots__/</code> 下留下带 <code>-win32</code> 后缀的文件。在宿主上安装 DejaVu 无法解决：字体只是差异之一，字形栅格化与子像素平滑仍与 Linux 不同，比对结果没有意义。这条失败是预期结果，同一批浏览器态用例中的无障碍与浮层定位在宿主上照常通过。</p><p>修改皮肤的工作方式：</p><ul><li>本地 <code>pnpm visual:baseline</code> 查看本次改动影响的截图，差异图在 <code>.vitest-attachments/</code> 下逐张打开；</li><li>PR 的判据是 CI 的 <code>browser</code> job，本地 <code>pnpm test:browser</code> 的固定失败项可以忽略；</li><li>CI 失败时先下载 <code>visual-diffs</code> artifact 查看，确认是有意的视觉改动，再 <code>pnpm visual:baseline --update</code> 重新生成基线并提交。</li></ul><p>字体族名、安装它的 apt 包、容器镜像与运行命令分散在用例、容器脚本、CI 与本页中，任何一处不一致都只表现为四十张整体判红。<code>check-visual-baseline-env</code> 把四处对齐，并核对镜像版本与 <code>pnpm-workspace.yaml</code> 中 <code>playwright</code> 的版本一致、CI 的 <code>browser</code> job 中安装字体的步骤排在运行用例之前。</p><h3 id="基线变更必须经人工审阅" tabindex="-1">基线变更必须经人工审阅 <a class="header-anchor" href="#基线变更必须经人工审阅" aria-label="Permalink to &quot;基线变更必须经人工审阅&quot;">​</a></h3><p>基线的更新是无声的：<code>git diff</code> 只显示二进制文件变化，看不出变成了什么。任何人都可以 <code>--update</code>，把一次真实的视觉回归记录为新基线。</p><p>因此规则是：PR 中凡有 <code>__screenshots__/</code> 下的改动，作者必须逐张说明变更原因，审阅者必须打开图片查看。改动张数与改动理由不一致的 PR 不合并。</p><p>基线文件名带浏览器与平台后缀（<code>…-chromium-linux.png</code>），入库的只有 <code>linux</code> 一套。带其他平台后缀的文件不应出现（字体守卫会在生成之前拦截用例），出现时直接删除，不提交。</p><h2 id="视觉样板性能预算" tabindex="-1">视觉样板性能预算 <a class="header-anchor" href="#视觉样板性能预算" aria-label="Permalink to &quot;视觉样板性能预算&quot;">​</a></h2><p><code>pnpm visual:performance</code> 在与像素基线相同的 Playwright Linux 镜像中运行真实 Chromium，并把报告写到 <code>ui/packages/adapters/vue/.vitest-attachments/visual-performance.json</code>。这条用例不另建展示页：它直接复用 <code>visual-baseline.spec.ts</code> 的 Dialog <code>variant=&quot;blur&quot;</code> 夹具与 800×520 视口，在对话框后方逐帧移动固定渐变背景， 让 <code>backdrop-filter</code> 参与真实合成。默认透明与 <code>data-transparency=&quot;reduce&quot;</code> 交替采样，避免先后顺序把机器热态集中到某一档。</p><p>设备档固定为 <code>mcr.microsoft.com/playwright:v1.62.0-noble</code>、Chromium、800×520@1x、2 CPU、2 GiB， 并通过 CDP 对渲染进程施加 4× CPU throttle。每档先预热 30 帧，再采 120 帧，重复三轮。预算真源是 <code>tooling/scripts/visual-performance-budget.json</code>；容器镜像版本继续与 <code>pnpm-workspace.yaml</code> 的 Playwright catalog 对齐。</p><p>报告与门禁包含：</p><ul><li><code>requestAnimationFrame</code> 相邻回调的 p50 / p95 / 最大间隔；每帧都会改变背景绘制位置，这个间隔包含该帧排队、样式、绘制与合成对下一帧的影响，不把纯 JS 计时冒充 GPU 时间；</li><li>W3C Long Tasks <code>PerformanceObserver</code> 给出的长任务数量、最大值与合计时长；</li><li>每个可见且计算样式含 <code>blur(...)</code> 的 <code>backdrop-filter</code> 元素，在视口内裁切后的面积之和。重叠材质逐层计数，因为每一层都要独立取样；<code>reduce</code> 档必须严格为 0；</li><li>10 次交替挂载/卸载后，强制 GC 前后的 CDP <code>Memory.getDOMCounters</code> 差值，作为 documents / DOM nodes / JS listeners 的资源留存代理。</li></ul><p>这里不输出伪内存数。浏览器目前没有稳定的标准轨页面内存 API；候选 <code>measureUserAgentSpecificMemory()</code> 仍是 WICG 草案，且要求跨源隔离。测试页不为获取一个数字而伪造隔离条件， 报告如实记录候选 API 与 <code>crossOriginIsolated</code> 状态，并用上述 DOM 资源留存代理监测泄漏。长任务定义见 <a href="https://www.w3.org/TR/longtasks-1/" target="_blank" rel="noreferrer">W3C Long Tasks</a>，内存候选 API 的标准状态见 <a href="https://wicg.github.io/performance-measure-memory/" target="_blank" rel="noreferrer">WICG Measure Memory</a>。</p><p>第一次建立或有意重定预算时，必须先运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> visual:performance</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --record</span></span></code></pre></div><p><code>--record</code> 只写真实报告、不判断 <code>limits</code>。至少检查一份完整报告后，才按实测值与明确余量修改预算真源；常规 <code>pnpm visual:performance</code> 执行红线。CI 运行后者。像素基线仍由 <code>pnpm visual:baseline</code> 守护，本项不自动更新任何 PNG。</p><p>包体积不在性能 JSON 里复制数字：JavaScript / 发布产物继续由 <code>.size-limit.json</code> 与 <code>pnpm size</code> 守护，逐皮肤 CSS 继续由 <code>.size-limit.css.json</code> 与 <code>check-skin-size</code> 守护。性能预算门禁反查这两份真源；修改本项不得提高既有阈值。</p><h2 id="结构门禁" tabindex="-1">结构门禁 <a class="header-anchor" href="#结构门禁" aria-label="Permalink to &quot;结构门禁&quot;">​</a></h2><p><code>pnpm gate</code> 运行 123 项结构检查，它们检查的是判据无法覆盖的问题：静默失效、悬空承诺、未被命名的决策：</p><table tabindex="0"><thead><tr><th>门禁</th><th>拦截内容</th></tr></thead><tbody><tr><td><code>check-runtime-deps</code></td><td>库包的运行时依赖引入了未登记的第三方</td></tr><tr><td><code>check-exact-pins</code></td><td>库包依赖内联了版本号（只允许 <code>catalog:</code> / <code>workspace:</code>）</td></tr><tr><td><code>check-tokens-dist</code></td><td>令牌产物未入库</td></tr><tr><td><code>check-overlay-strategy</code></td><td>浮层坐标系在机器 / <code>connect</code> / 皮肤三处不一致</td></tr><tr><td><code>check-token-refs</code></td><td>皮肤引用了不存在的令牌名（整条声明会静默失效）</td></tr><tr><td><code>check-tone-tokens</code></td><td>语气轴对外的 <code>--xh-tone-*</code> 没声明在语气层的 <code>[data-tone]</code> 上，或与私有槽的取值分叉</td></tr><tr><td><code>check-shared-slots</code></td><td>同一字面量在多个组件中作为默认值，却未建立语义令牌</td></tr><tr><td><code>check-motion-easing</code></td><td>皮肤的缓动下探到 <code>--xh-ease-*</code> 原语、手写 <code>cubic-bezier()</code>，或使用了未登记的字面曲线</td></tr><tr><td><code>check-motion-amplitude</code></td><td>位移与缩放的幅度写成字面量（减弱动效档把 <code>--xh-motion-distance-*</code> 压成 <code>0px</code>、<code>--xh-motion-scale-*</code> 压成 <code>1</code>，写死的位置无法压缩）；居中用的百分比与 <code>0</code> / <code>1</code> 是几何，不在此列</td></tr><tr><td><code>check-disabled-contrast</code></td><td>禁用态前景色令牌上叠加 <code>opacity</code>，对比度被压到无法阅读</td></tr><tr><td><code>check-color-literals</code></td><td>颜色写死在 <code>background</code> 简写、<code>box-shadow</code> 颜色位或 <code>--xh-*</code> 槽赋值中：stylelint 的六个长属性白名单不覆盖这些位置</td></tr><tr><td><code>check-print-surface</code></td><td>浮层定位层 / 遮罩 / 滚动条 / 固定在视口上的节点在打印时未收起，或投影未由令牌层取消</td></tr><tr><td><code>check-placeholder-fg</code></td><td>占位文字两条通道（<code>::placeholder</code> 与 <code>[data-placeholder]</code>）取了不同的默认前景，或用 <code>opacity</code> 表达深浅</td></tr><tr><td><code>check-autofill</code></td><td>渲染原生表单控件的输入框未写自动填充规则，或两种手段 / 两个引擎的选择器缺一</td></tr><tr><td><code>check-part-wiring</code></td><td>解剖声明、<code>connect</code> 产出、适配器却未接线的部件</td></tr><tr><td><code>check-dead-state-attr</code></td><td><code>connect</code> 发出的 <code>data-*</code> 在本组件的作用域中没有任何规则消费：其他组件的同名规则不计入，该规则永远无法选中它。信息钩子逐条登记，登记项过期同样判失败</td></tr><tr><td><code>check-skin-parts</code></td><td>皮肤选择器中的 <code>[data-part]</code> 不在所属 scope 的解剖中：部件退役后遗留的规则永远无法选中节点，<code>surface:update</code> 还会把它的覆盖槽收回公开面。scope 按选择器计算（逐分支、逐复合：写在 <code>[data-part]</code> 前后的 <code>[data-scope]</code> 都约束这一节，未写的沿用左侧最近一节，<code>:is()</code> / <code>:where()</code> 中一致的 scope 带回外层，<code>:not()</code> / <code>:has()</code> 中各自计算），解剖外的名字逐条登记，登记项过期同样判失败；<code>data-scope</code> / <code>data-part</code> 的属性选择器无法读取的（转义、匹配符不是全等）同样判失败</td></tr><tr><td><code>check-breakpoints</code></td><td>皮肤 <code>@media</code> 中的断点字面量不在令牌清单中（自定义属性在媒体条件中不生效，只能写字面量）</td></tr><tr><td><code>check-focus-ring</code></td><td>聚焦环的粗细、颜色、偏移写了字面量而不是令牌，主题与全局调整对它无效</td></tr><tr><td><code>check-focus-ring-surface</code></td><td>可聚焦部件的面与环的对比度不足 3:1（按计算结果，不按形态推断），该档却未把 <code>--xh-_ring-color</code> 设为 <code>currentColor</code>：键盘焦点在该面上等于未绘制。<code>currentColor</code> 覆盖到非实心档、<code>:focus-visible</code> 中关闭环（<code>outline: none</code> / <code>outline-width: 0</code>）却未登记环由谁绘制、绘制实心面却不接焦点也未登记的部件，同样判红；聚焦规则把环色写成透明的直接判红，没有登记表</td></tr><tr><td><code>check-focus-outline-reset</code></td><td>皮肤在 <code>:focus:not(:focus-visible)</code> 下复位 <code>outline</code>（含 <code>outline-style</code> / <code>outline-width</code> / <code>outline-color</code>）。UA 只在 <code>:focus-visible</code> 绘制环，这条复位是死代码，而 <code>outline</code> 简写会把 <code>outline-color</code> 复位成 <code>currentColor</code>，与家族配方的 <code>outline-color</code> 过渡叠加，焦点离开时闪出一圈近黑描边</td></tr><tr><td><code>check-exports</code></td><td>已实现却未从包级入口导出，包外无法获取，而构建与类型检查照常通过</td></tr><tr><td><code>check-package-roles</code></td><td>包所在的角色组与其 <code>package.json</code> 中的依赖声明不一致</td></tr><tr><td><code>check-public-surface</code></td><td>公开面基线中有而当前没有的名字：被删除或改名</td></tr><tr><td><code>check-visual-performance-budget</code></td><td>固定设备、默认/reduce 场景、真实浏览器入口与既有 JS/CSS 体积真源任一脱节</td></tr><tr><td><code>check-changeset-packages</code></td><td>changeset 头部写了 <code>.changeset/config.json</code> ignore 表里的私有包（如 <code>@xihan-ui/testing</code>）或不在工作区的包：前者与发布包混写时 <code>changeset version</code> 直接报 Mixed changesets，只写它时整份被丢掉、正文进不了 CHANGELOG；后者报 not in the workspace。不依赖 git 基线，头部读不成「包名: 档位」也判红</td></tr><tr><td><code>check-surface-edge</code> / <code>check-selection-marker</code> / <code>check-state-ladder</code> / <code>check-text-role</code> 与扩展后的 <code>check-elevation-role</code> / <code>check-shape-scale</code> / <code>check-press-feedback</code> / <code>check-family-parity</code></td><td>七条家族门禁：根面边界三选一、选中与当前态按语义分类、交互态按承载面阶梯、排版与图标按角色、raised 逐部件登记且必带描边、形状身份表、按压几何与换底、同族同值。尚未迁移的存量登在 <code>tooling/scripts/family-backlog.json</code>，每条必须真被放行过一次（登记了却没命中判过期），<code>check-family-backlog</code>（<code>gate:family</code> 里的 <code>family-backlog.spec.mjs</code>）把每段条目数钉在快照与 CEILING 上、键集合只许是快照的子集——表只减不增</td></tr></tbody></table><p>另有分层依赖检查与十一项单独的门禁：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> boundaries</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 分层依赖 + 禁循环 + styles 不依赖 JS + 库包不引入第三方</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:tokens</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 重新运行令牌生成后比对，修改源文件未运行生成会被拦截</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:styled</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 重新生成皮肤的无层版产物后比对</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:cem</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # 重新生成自定义元素清单后比对</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:docs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 重新生成组件文档页后比对</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:exports</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # 重新生成子路径导出后比对 package.json</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:surface</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # 公开面基线：基线中有而当前没有的名字判失败</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:demos</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 在真实 Chromium 中运行文档站的自定义元素示例</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:publish</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # 逐包运行 publint 与 attw，校验 exports 条件与类型解析</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:llms</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 文档站的机读资产：页数、组件数、令牌数与库对账，示例不得保留站点标签</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate:family</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 逐家族豁免表只减不增（条目数快照 + 键集合子集），并用临时夹具证七条家族门禁会红</span></span></code></pre></div><p><code>gate:llms</code> 读取文档站的构建产物，运行前先在 <code>docs/</code> 下运行一次 <code>pnpm build</code>。</p><p>文档站是独立工作区，lint 也独立，在 <code>docs/</code> 下运行 <code>pnpm lint</code>。示例语料的引号与分号与库源码相反，规则固定在 <code>docs/eslint.config.js</code> 中：示例供使用者复制，不跟随库源码的写法。</p><p><code>gate:publish</code> 按包声明的支持面校验：ESM-only、<code>engines.node &gt;= 18</code>，不提供 CJS，也不承诺 node10 的旧式解析。</p><h2 id="体积棘轮" tabindex="-1">体积棘轮 <a class="header-anchor" href="#体积棘轮" aria-label="Permalink to &quot;体积棘轮&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> size</span></span></code></pre></div><p>37 条产物各有上限（gzip 后），超出即失败。预算一律按实测留一成余量。逐条限额的真源是 <code>ui/.size-limit.json</code>，具体数字以该文件为准。</p><h2 id="相关" tabindex="-1">相关 <a class="header-anchor" href="#相关" aria-label="Permalink to &quot;相关&quot;">​</a></h2><ul><li><a href="./a11y">无障碍与键盘规格</a></li><li><a href="./position">浮层定位</a></li><li><a href="./../overview#分层与依赖矩阵">架构总览</a></li></ul>`,73)])])}const g=s(d,[["render",o]]);export{k as __pageData,g as default};
