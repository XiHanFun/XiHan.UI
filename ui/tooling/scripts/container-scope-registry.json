{
  "$comment": "容器查询的落点白名单。键是「组件:部件」，只有登在这里的部件才许在皮肤里写 container-type。判据：它是库自己渲的根、是块级盒、宽度由外部给，不是收缩包裹的。pending 是「已核准、这一批还没写」，写上了就把 pending 删掉。表为空是正常状态：库里现在一处 container-type 都没有，全部换档走视口断点 @media。",
  "containers": {}
}
