# @xihan-ui/react

XiHan.UI 的 React 适配器。组件是 `Xh*` 大驼峰扁平导出，与 Vue 侧逐字同名；
带载荷的插槽落成函数式 children。

只支持 React 19。

```bash
pnpm add @xihan-ui/react @xihan-ui/styles @xihan-ui/tokens
```

文档：<https://ui.docs.xihanfun.com>
