---
'@xihan-ui/headless': minor
---

Anchor 点了目录项平滑滚动时，锁住高亮直到滚动真正停稳：逐帧读滚动位置，动过之后几帧不再变即解锁，一直没动过（目标本在视野里、或减弱动效下瞬移）则多等十几帧。此前固定锁 1000ms，长距离滚动还没到、锁已经松开，途中扫过的区块会把高亮冲掉。机器事件 `after.scrollLock` 改名 `SCROLL.SETTLE`。平滑滚动按滚动目标所在的作用域判断减弱动效。
