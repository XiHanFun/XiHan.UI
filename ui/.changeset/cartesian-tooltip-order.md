---
'@xihan-ui/headless': minor
'@xihan-ui/vue': minor
'@xihan-ui/react': minor
'@xihan-ui/web-components': minor
---

直角坐标图新增 `tooltipOrder`（Web Components 属性 `tooltip-order`）：`series` 按图例次序（缺省），`descending` / `ascending` 按数值排提示框里的行，缺失值排在最后；回调里的 `items` 仍按图例次序。
