---
'@xihan-ui/tokens': minor
---

新增原语 `--xh-color-neutral-650`（600 与 700 之间的半档），深色档的 `--xh-bg-subtle-active`（淡底承载阶梯的按下面）改取它：按住且落焦时焦点环保留在按下面上，默认环 `brand.400` 压 600 只有 2.93:1，压 650 是 3.39:1，与浅色档 300 上的 3.43:1 同一余量。
