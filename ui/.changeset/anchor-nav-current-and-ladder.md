---
'@xihan-ui/styles': minor
---

Anchor 按设计真源归位导航当前页与阶梯：当前节字色缺省由 `--xh-fg-brand` 改 `--xh-fg-brand-strong`（`--xh-anchor-link-fg-current` 覆盖槽不变，语气组仍取语气文字档）；链接悬停由 `--xh-bg-subtle-hover` 改白底承载的 `--xh-bg-subtle`（100），新增按下面 `--xh-anchor-link-bg-pressed` 缺省 `--xh-bg-subtle-hover`（200），只换面不缩放，高对比档按住用系统高亮反色画回。
