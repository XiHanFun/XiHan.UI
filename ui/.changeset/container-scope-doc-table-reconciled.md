---
"@xihan-ui/styles": patch
---

`docs/guide/styling.md` 里那张「哪几个根是查询容器」的表接上 `check-doc-numbers`，真源是 `container-scope-registry.json` 的条数。

这张表在写下的当轮就过时过一次：`field` 与 `steps` 在初稿之后才退回第一层、把自己那条从登记表整条删掉，全靠人回头又查了一遍才发现。进出登记表是常事，表没人看管就会静默说谎。
