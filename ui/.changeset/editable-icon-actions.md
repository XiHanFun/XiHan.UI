---
'@xihan-ui/styles': minor
'@xihan-ui/tokens': minor
---

Editable 改为与字段控件一致的上下层级：标签在上，预览文字或输入框占据内容行左侧，动作组固定在右侧。预览态只显示编辑图标，编辑态只显示确认与取消图标；空按钮由共享皮肤使用 Pencil、Check、Close 字形绘制，作者仍需提供可访问名称。

新增字段式排布、三颗图标动作、粗指针命中区与状态样式后，`editable.css` 的压缩前体积由 10,386 字节增至 12,165 字节。
