---
'@xihan-ui/headless': minor
---

ColorSwatchPicker 的 `label` 部件新增投影 `data-disabled`：整组 `disabled` 时标签随之置灰（皮肤的 `--xh-color-swatch-picker-label-fg-disabled` 从此有了真实的消费状态）；单格禁用只是那一格的事，不写到标签上。三端与一致性套件同步断言。
