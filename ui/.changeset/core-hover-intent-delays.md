---
'@xihan-ui/core': minor
---

新增导出 `HOVER_INTENT_OPEN_DELAY`（100）与 `HOVER_INTENT_CLOSE_DELAY`（300）：`trackHoverIntent` 的 `openDelay` / `closeDelay` 缺省值改由这两个具名常量给出，取值不变。依赖悬停意图时长的组件（Menu 子菜单缺省、SideNav 折叠态悬停弹出）与它同源。
