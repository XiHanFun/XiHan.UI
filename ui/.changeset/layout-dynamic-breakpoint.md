---
'@xihan-ui/headless': major
---

Layout 的 `siderBreakpoint` 现在跟随运行期属性变化切换媒体查询，移除档位时解除观察并清除窄屏状态。
切入 sheet 时应用当前档位，受控侧栏仍只发开合意图。旧查询和停机后的迟到事件不会再写回。

指定断点时必须提供对应 CSS 断点令牌和所属 Window.matchMedia；缺失时明确失败，不再静默跳过。
监听登记、切换和通知失败会清理取得的资源，下一次有效属性更新可重新建立观察。
