---
'@xihan-ui/core': patch
---

FocusScope 的挂载和卸载自动聚焦事件现在由 DOM 监听器与选项回调共享，且每个生命周期只派发一次。任一通道调用 `preventDefault()` 都能阻止默认聚焦或焦点归还；DOM 监听器已取消时，选项回调仍会收到同一枚事件。

卸载通知不再被 `restoreFocus=false` 或更新层短路，事件始终发给首次挂载的容器；从未取得容器时不会伪造 body 事件。挂载回调抛错会先回滚焦点域资源；事件使用 scope 所属窗口提供的构造器。
