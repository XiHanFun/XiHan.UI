---
'@xihan-ui/styles': patch
---

TagGroup 里标签的悬停 / 按下面带 `[data-tone]` 限定：没写语气的实心标签落在带 `data-tone` 的组里，按下不再读祖先的 `--xh-_tone-active` 跳成另一族；写了语气的淡底标签悬停 / 按下沿语气曲线走 20% / 28%，不再跳回中性阶梯丢掉语气。

皮肤体积 5735 → 6448 字节：涨在语气标签悬停 / 按下的两条 [data-tone] 限定规则与实心按下拆成的缺省 / 语气两条。
